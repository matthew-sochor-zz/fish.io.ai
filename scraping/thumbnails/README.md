# Scrape some URLs for some fishies pics

# Prerequisites

`pip install selenium`

and (at least on mac)

`brew install geckodriver`

and 

'brew cask install firefox'

or test your luck with:

`make install`

Now, get scraping with:

`make`
